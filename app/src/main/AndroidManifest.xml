<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.netmania.checklod">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChecklodSetup">

        <activity
            android:name=".presentation.activity.MainActivity"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.activity.MultiDeviceActivity" />
        <activity android:name=".presentation.activity.MultiDeviceTempActivity" />
        <activity android:name=".presentation.activity.ReportActivity" />
        <activity android:name=".presentation.activity.DeviceListActivity" />
        <activity android:name=".presentation.activity.InterfaceActivity" />
        <activity android:name=".presentation.activity.OffsetActivity" />
        <activity android:name=".presentation.activity.LoggerListActivity" />
        <activity android:name=".presentation.activity.DataSyncActivity" />
        <activity android:name=".presentation.activity.TimeSetupActivity"></activity>
    </application>

</manifest>